// Stub file.
//
// The original implementation of `ReaderSmartPrefetch` was lost: the source file
// had been replaced by a tool offload placeholder ("[CONTEXT OFFLOADED] ...")
// in the upstream repository's `source-backup` branch, and the real content was
// never committed. The class is not referenced by any other code in this build,
// so a minimal stub keeps the package graph valid.
class ReaderSmartPrefetch {}
